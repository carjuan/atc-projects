# Rem units vs pixel values

> Check CSS for classes on `body`, `.cards` and `.cards-option` to talk about the difference between the rem values over pixels values for user settings font-size preference.
